[package]
name = "renegade"
version = "0.2.0"
edition = "2021"
description = "A nonparametric supervised learning library based on KNN"
license = "AGPL-3.0-or-later"

[dependencies]
ordered-float = "4"
pav_regression = "0.7"

[dev-dependencies]
rand = { version = "0.8", features = ["small_rng"] }
